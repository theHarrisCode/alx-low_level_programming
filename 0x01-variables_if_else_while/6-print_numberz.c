#include <stdio.h>
/**
 * main - prints all single digits in base 10
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 30; i <= 39; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
